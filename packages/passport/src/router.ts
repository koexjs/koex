// import * as router from '@koex/router';
// import { IPassport } from './passport';

// export function router(passport: IPassport) {
//   router.get('/auth/:strategy', passport.authenticate);
//   router.get('/auth/:strategy/callback', passport.callback);
// }